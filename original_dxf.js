import DxfParser from 'dxf-parser'; import { transformToWGS84, calculatePolygonArea, closeRing, isClosedRing } from './geoUtils.js';  export const parseDXF = async (file, husoSelection) => {   const text = await file.text();   const parser = new DxfParser();   let dxf;   try {     dxf = parser.parseSync(text);   } catch (error) {     throw new Error("El fichero no parece ser un DXF v├ílido o est├í corrupto.");   }    const parcels = [];   const texts = [];    // DXF has no strict EPSG inside it normally in this context, we require the user's selected HUSO   const epsgCode = husoSelection || "25830";    let autoIndex = 0;    function generateAutoName(index) {       const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";       const number = Math.floor(index / letters.length) + 1;       const letter = letters[index % letters.length];       return `${number}${letter}`;   }    // Ray-casting point in polygon algorithm exactly as provided   const isPointInPolygon = (point, polygon) => {       let x = point[0], y = point[1];       let inside = false;       for (let i = 0, j = polygon.length - 1; i < polygon.length; j = i++) {           let xi = polygon[i][0], yi = polygon[i][1];           let xj = polygon[j][0], yj = polygon[j][1];           let intersect = ((yi > y) !== (yj > y)) && (x < (xj - xi) * (y - yi) / (yj - yi) + xi);           if (intersect) inside = !inside;       }       return inside;   };    // 1. Recoger todos los textos   dxf.entities.forEach(entity => {       if (entity.type === "TEXT" || entity.type === "MTEXT") {           if (!entity.text) return;            // Text position can be in position, startPoint, or flat x/y depending on DXF version           const px = entity.position ? entity.position.x : (entity.startPoint ? entity.startPoint.x : entity.x);           const py = entity.position ? entity.position.y : (entity.startPoint ? entity.startPoint.y : entity.y);            if (px === undefined || py === undefined) return;            texts.push({               text: entity.text.trim(),               position: [px, py]           });       }   });    dxf.entities.forEach(entity => {     if (entity.type !== "LWPOLYLINE" && entity.type !== "POLYLINE") return;      if (!entity.vertices || entity.vertices.length < 3) return;      let coords = entity.vertices.map(v => [v.x, v.y]);      // Force geometrically closing any polyline since many CAD workflows     // leave adjacent interior geometries structurally unclosed.     const first = coords[0];     const last = coords[coords.length - 1];      if (Math.abs(first[0] - last[0]) > 0.001 || Math.abs(first[1] - last[1]) > 0.001) {         coords.push([...first]); // Ensure closing point is added     }      let name = null;      // buscar texto dentro de la parcela     for (let t of texts) {         if (isPointInPolygon(t.position, coords)) {             const valid = t.text.match(/(FINCA|_MOD|PARCELA|FR-|REF|RECINTO)/i) || t.text.trim().length === 14;              if (valid) {                 name = t.text.trim().replace(/\s/g, "");                 break;             }         }     }      // si no hay texto v├ílido     if (!name) {         name = generateAutoName(autoIndex);         autoIndex++;     }      const area = calculatePolygonArea(coords);     const geometryCoords = transformToWGS84(coords, epsgCode);      parcels.push({         id: `dxf-${file.name}-${parcels.length}`,   // unique: filename + sequential index         name: name,         area: area,         filename: file.name,         huso: epsgCode,         originalCoords: [coords],          geometry: {             type: "Polygon",             coordinates: [geometryCoords]         }     });    });    // Filtrar duplicados exactos (mismo ├írea y mismo v├®rtice inicial)   // Es muy com├║n que los programas CAD exporten geometr├¡as solapadas   const uniqueParcels = [];   parcels.forEach(p => {       let isDuplicate = false;       for (const u of uniqueParcels) {           if (Math.abs(p.area - u.area) < 0.1) {               const ringA = p.originalCoords[0];               const ringB = u.originalCoords[0];               if (ringA.length === ringB.length) {                   const diffX = Math.abs(ringA[0][0] - ringB[0][0]);                   const diffY = Math.abs(ringA[0][1] - ringB[0][1]);                   if (diffX < 0.05 && diffY < 0.05) {                       isDuplicate = true;                       break;                   }               }           }       }       if (!isDuplicate) {           uniqueParcels.push(p);       }   });    return uniqueParcels; };